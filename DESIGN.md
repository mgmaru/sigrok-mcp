# sigrok-mcp 設計書

## 目的

ロジックアナライザ・オシロスコープを AI から自然言語で操作するための MCP サーバを開発し、OSS として公開する。

このドキュメントは、開発者（Hiroaki および将来のコントリビュータ）が設計判断の背景を理解できるように、思考過程を残すことを目的とする。

## 背景

### 解きたい課題

測定器の世界には以下の課題がある。

1. **ハードウェアとソフトウェアが 1 対 1 で縛られている**: 安価な測定器を買っても付属ソフトが貧弱で、UX が改善しない
2. **AI 連携がほぼ存在しない**: LLM の活用が他ドメインに比べて極端に遅れている
3. **AI 活用と測定の決定論性が衝突する**: 確率的に振る舞う LLM は、再現性とトレーサビリティが要求される測定の世界と本来相性が悪い

### 既存の解決策とその限界

- **sigrok / PulseView**: 200 機種以上を共通 API で抽象化する OSS。優れているが UI が古く、AI 連携はゼロ
- **PyVISA / LabVIEW / MATLAB**: 高機能だが、生のスクリプト操作が中心で AI 連携の仕組みがない
- **商用 AI オシロ（Keysight 等）**: 機能はあるが特定機種に縛られ、高価

### 本プロジェクトの位置づけ

sigrok の 200 機種対応という資産を活かし、その上に **AI が呼び出せる薄い MCP レイヤー**を被せる。これにより以下が実現する。

- 自分で GUI を作らずに、Claude Desktop 等を UI として使える
- libsigrok の機種対応をそのまま継承できる（個人で 200 機種は不可能）
- 開発スコープを「抽象化レイヤー + AI ツール定義」に集中できる

## 設計原則

### 原則 1: LLM の役割を限定する

LLM は **意図の翻訳と道具選び** だけを担当する。ハードウェアを直接判断したり、測定値を確率的に「推定」したりはしない。

```
ユーザの自然言語  ──→  LLM  ──→  どのツールをどの順で呼ぶか
                                          ↓
                              MCP ツール（決定論的なコード）
                                          ↓
                                    libsigrok（決定論的）
                                          ↓
                                       実機
```

### 原則 2: 測定結果の権威はハードウェアに置く

LLM が「だいたいこんな波形でしょう」と推測することは絶対にさせない。波形データは libsigrok が取得した値が唯一の真実であり、LLM はその要約と解釈を支援するだけ。

### 原則 3: 危険な操作には人間の承認を挟む

電圧設定、信号発生器の出力、トリガレベルの大幅変更など、誤ると機材や被測定物を傷つける可能性のある操作は、MCP ツールの仕様として承認ゲートを設ける。

### 原則 4: トレーサビリティを保つ

何を、いつ、どの設定で測定したかを構造化ログに残す。これは「校正のトレーサビリティ」という測定文化の核を裏切らないため。

### 原則 5: 小さく始める

最初から 200 機種対応・全プロトコル対応を目指さない。最小構成で動かし、コミュニティのフィードバックで広げる。

## アーキテクチャ

### 層構造

```
┌─────────────────────────────────┐
│ Claude Desktop / Cursor 等        │  ← 既存の MCP クライアント
└─────────────────────────────────┘
                ↕ MCP プロトコル
┌─────────────────────────────────┐
│ sigrok-mcp（本プロジェクト）         │
│  - ツール定義                      │
│  - パラメータ検証                   │
│  - 波形データの要約                 │
│  - 構造化ログ                      │
└─────────────────────────────────┘
                ↕ Python バインディング
┌─────────────────────────────────┐
│ libsigrok                       │  ← 既存 OSS（GPLv3）
│  - 200 機種のドライバ              │
│  - プロトコルデコーダ                │
└─────────────────────────────────┘
                ↕ USB / TCP / Serial
┌─────────────────────────────────┐
│ 物理測定器                        │
└─────────────────────────────────┘
```

### モジュール構成（暫定）

```
sigrok_mcp/
├── server.py          # MCP サーバ本体（エントリポイント）
├── tools/             # 各 MCP ツール
│   ├── devices.py     # list_devices
│   ├── capture.py     # capture_waveform
│   ├── analysis.py    # get_waveform_summary
│   ├── decode.py      # decode_protocol
│   └── export.py      # export_waveform
├── adapters/          # libsigrok との接続層
│   └── sigrok_adapter.py
├── safety/            # 安全機構
│   └── validator.py   # パラメータ検証、危険操作の検出
├── logging/           # 構造化ログ
│   └── audit.py
└── schemas/           # 入出力スキーマ定義
    └── waveform.py
```

## 提供する MCP ツール（最小構成）

### `list_devices`

接続されている測定器を列挙する。

- 入力: なし
- 出力: 機種名、ベンダ、シリアル、対応チャネル数などのリスト

### `capture_waveform`

指定設定で波形を取得する。

- 入力: チャネル、サンプリングレート、キャプチャ時間、トリガ条件
- 出力: 波形データ（ファイルパスまたは要約 + プレビュー画像）

LLM に大量の生サンプルを渡すとトークンが爆発するため、**生波形は LLM に渡さない**。代わりに：
- 波形ファイルをローカルに保存し、パスのみ返す
- 自動で特徴量サマリ（最大値、最小値、周波数等）を抽出して返す
- 必要に応じて縮小したプレビュー画像を生成

### `get_waveform_summary`

波形ファイルから特徴量を抽出する。

- 入力: 波形ファイルパス、抽出したい指標
- 出力: 周波数、立ち上がり時間、ジッタ、FFT 結果などの構造化データ

### `decode_protocol`

プロトコルを解読する。libsigrok のプロトコルデコーダ（I2C / SPI / UART / CAN 等多数）を呼び出す。

- 入力: 波形ファイルパス、プロトコル名、各信号線のチャネル割当
- 出力: 構造化された通信ログ（タイムスタンプ、データ、エラー）

### `export_waveform`

波形を別形式に変換・保存する。

- 入力: 波形ファイルパス、出力フォーマット（CSV / VCD / sr 等）
- 出力: 出力ファイルパス

## 安全機構

### パラメータ検証

各ツールの入力パラメータは、関数呼び出しの最初に検証する。

- サンプリングレートが機種の上限を超えていないか
- キャプチャ時間が極端に長くないか（メモリ枯渇防止）
- チャネル番号が存在するか

検証に失敗した場合、`ValidationError` を返す。LLM はエラーメッセージを読んで再試行できる。

### 危険操作の承認ゲート

電圧出力や信号発生など、被測定物に影響する操作には、ツール仕様として「`confirm: true` を渡さないと実行しない」というガードを設ける。LLM は最初の呼び出しで「この操作は承認が必要」というエラーを受け取り、ユーザに確認を求める設計にする。

ただし、最小構成では「波形を取る」だけなので、この機構は将来的な拡張として用意しておく。

## トレーサビリティ

すべての測定操作を JSONL 形式の監査ログに記録する。

```json
{"timestamp": "2026-05-10T12:00:00Z", "tool": "capture_waveform",
 "args": {"channel": 0, "samplerate": 1000000, "duration": 1.0},
 "device": {"vendor": "Hantek", "model": "6022BE", "serial": "..."},
 "output_file": "/tmp/capture_001.sr",
 "user_request": "I2C の波形を取って解読して"}
```

これにより、後から「いつ、どの機材で、どう設定して測ったか」が完全に追跡できる。校正トレーサビリティを要求する研究室や品質保証用途でも使えるレベルを目指す。

## 開発フェーズ

### Phase 0: 環境準備（〜2 週間）

- Hantek 6022BE を購入（数千円）
- libsigrok と PulseView をインストールし、実機で動かす
- 「ユーザとしての痛み」を肌で理解する
- この期間に得た不満は GitHub の Issue として記録

### Phase 1: 最小動作版（1〜1.5 ヶ月）

- MCP 公式 Python SDK で雛形を作る
- libsigrok の Python バインディングで `list_devices` だけ実装
- demo driver で動作確認
- Claude Desktop に登録して、自然言語から `list_devices` が呼べることを確認

ここで動くものが見えるとモチベーションが大きく上がる。

### Phase 2: コア機能（1〜2 ヶ月）

- `capture_waveform` を実装
- `get_waveform_summary` を実装（NumPy / SciPy で特徴量抽出）
- Hantek 6022BE で実機動作確認
- README とデモ動画を整備し、GitHub に **早期公開**

### Phase 3: プロトコルデコード（1 ヶ月）

- `decode_protocol` を実装（libsigrok のプロトコルデコーダ呼び出し）
- I2C / SPI / UART で動作確認
- note.com で技術記事を書く（日本語）
- Hacker News や Reddit r/electronics で英語紹介

### Phase 4: 拡張と運用（継続）

- ユーザの Issue に応じて機種別の動作確認を進める
- `export_waveform` 等の補助ツール追加
- 安全機構を実装
- コミュニティからの PR を受ける体制整備

## 公開戦略

### リポジトリの初期セット

- `README.md` / `README.ja.md`: 30 秒で何ができるかを伝える
- `DESIGN.md`: 本ドキュメント
- `INSTALL.md`: インストール手順
- `CONTRIBUTING.md`: 機種追加・PR の作法
- `LICENSE`: GPLv3
- `.github/workflows/`: CI（demo driver でテスト）
- デモ動画 or GIF を README に埋め込む

### 命名

`sigrok-mcp`。理由:
- sigrok キーワードで sigrok コミュニティに発見されやすい
- 何をするものか名前から想像できる
- 短くて覚えやすい

### 公開タイミング

**Phase 2 の終わりに早期公開**。完璧を目指さず、最低限「Hantek 6022 で波形が取れる」段階で出す。完成を待つ間に他の人に先を越されるリスクのほうが大きい。

### 配布

- PyPI に登録: `pip install sigrok-mcp` で入る状態を目標
- GitHub Releases にバイナリ配布は不要（Python なら）
- MCP Marketplace（公式ディレクトリ）に登録

### 発信

- note.com 技術アカウント: 日本語で開発記録と技術解説
- X / Mastodon: 開発の途中経過を断続的に
- sigrok 公式 ML / IRC: 完成時にアナウンス（コミュニティへの礼儀）
- Hacker News / Reddit r/electronics: 英語で紹介

## ライセンスの判断

GPLv3 を採用する。

### 理由

libsigrok が GPLv3 のため、それを使う本プロジェクトもコピーレフトを継承する必要がある。回避するには libsigrok を使わず自分で 200 機種のドライバを書く必要があるが、これは個人では非現実的。

### トレードオフ

- 純粋な OSS としては GPLv3 で問題ない
- 将来「商用版を有料で売る」「クローズドな企業内利用に組み込む」道は閉ざされる
- ただし、サポート契約・コンサル・関連ツール開発などでの収益化は可能

### 将来の選択肢

もし将来、商業化を検討するなら：
- 別プロジェクトとして「SCPI 対応機種専用の MCP サーバ」を MIT / Apache で立ち上げる
- libsigrok 非依存のため、ライセンスの自由度が高い
- 対応機種は減るが、高級機種ユーザ層を狙える

## リスクと対応

### リスク 1: libsigrok の Python バインディングが古い・不安定

**対応**: Phase 0 の段階で libsigrok / python-sigrok の最新状況を確認する。問題があれば、libsigrok の C API を直接叩く Rust 実装も検討（ただし開発コストは増える）。

### リスク 2: 同様のプロジェクトが既に存在する

**対応**: 公開前に GitHub と MCP Marketplace を再調査する。類似があれば、そちらにコントリビュートするか、明確に差別化（例: 日本語特化、特定機種特化）する。

### リスク 3: ユーザがつかない

**対応**: 最初の半年はスター数を気にしない。日本語コミュニティと sigrok コミュニティの両方に少しずつ露出していく。最低限、自分が普段使えるツールにはなる。

### リスク 4: メンテナンス負担

**対応**: 月 10 時間程度の見込みで運用する。Issue 対応の SLA は明示しない。「ベストエフォート」の OSS であることを README に明記。

## 未決事項

設計が進む過程で決める必要があるが、現時点で結論を出さない項目。

- 波形データの中間ファイル形式（sigrok の `.sr` 形式をそのまま使うか、独自フォーマットを定義するか）
- 大容量波形の扱い（数 GB の波形を LLM にどう要約して渡すか）
- マルチデバイス同時操作のサポート範囲
- 安全機構の実装タイミング（Phase 2 か Phase 4 か）

これらは実装が進む中で随時判断し、本ドキュメントに追記する。

## 参考資料

- [sigrok 公式](https://sigrok.org/)
- [Model Context Protocol 仕様](https://modelcontextprotocol.io/)
- [AICEBERG (MDPI 2026)](https://www.mdpi.com/2624-6511/9/5/73): LLM + MCP + SCPI で RF スペクトラムアナライザを制御する研究。本プロジェクトの設計思想と近い
- [WaveForms Control MCP](https://mcpmarket.com/server/waveforms-control): Digilent 製品向けの先行 MCP 実装

## 改訂履歴

- v0.1（2026-05-10）: 初版
